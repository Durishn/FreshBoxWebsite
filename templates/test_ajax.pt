<div metal:use-macro="view.global_template">
    <div metal:fill-slot="content">
        <button onClick="$(function(){$.getJSON('/test.json', function(data) {$.each(data, function (key, val) {alert(val);});});});">Click me</button>
    </div>
</div>